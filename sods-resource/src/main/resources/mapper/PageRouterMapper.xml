<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="org.sods.resource.mapper.PageRouterMapper">

    <select id="selectPageDataByURLAndLan" resultType="java.lang.String">
        select page_data from PageData, PageMapper
        where PageData.page_id = PageMapper.page_id
        and PageMapper.url = #{url}
        and PageMapper.language = #{lang}
    </select>
</mapper>